; reduce to core intrinsics, implement things like map
; in r7rs.scm

for binding to runtime/expander when wasm:
either allow for jit imports or special case expander or runtime
then map globals back to library bindings
maybe some loki-current-expander fake imports
