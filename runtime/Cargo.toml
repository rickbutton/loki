[package]
name = "runtime"
version = "0.0.1"
authors = ["Rick Button <me@rickbutton.me>"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
lazy_static = "1.3.0"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
