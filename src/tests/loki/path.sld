(define-library (tests loki path)
(import (scheme base))
(import (loki path))
(import (srfi 64))
(import (tests util))
(begin
(define-tests run-tests-loki-path "(loki path)"

(test-group "make-posix-path"

)

)))
